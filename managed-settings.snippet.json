{
  "//": "HARD MODEL GATE — belt-and-suspenders on top of the frontmatter + PreToolUse hook. `availableModels` is enforced everywhere a model can be named (docs/en/model-config 'Restrict model selection'): /model, --model, ANTHROPIC_MODEL, resume, subagent frontmatter, the Agent tool's model param, AND CLAUDE_CODE_SUBAGENT_MODEL. An excluded subagent value is skipped and the subagent runs on the inherited model instead.",

  "//TRADEOFF-READ-FIRST": "This allowlist CANNOT distinguish 'fable as my watched lead session' (allowed by the bundle's rubric) from 'fable as a subagent' (never allowed). Excluding fable here blocks BOTH — /model fable will be refused. Pick one: (a) STRICT (this file as-is): fable nowhere, maximum safety, rubric's lead-session use gone; (b) LEAD-ALLOWED: add \"fable\" to the list below and rely on the PreToolUse hook for subagent gating — note that with a fable LEAD session, a blocked subagent value falls back to the INHERITED model (i.e. fable), so the hook is the only thing standing between you and fable subagents in that configuration.",

  "//WHERE": "Hand-place at your OS managed-settings path (docs/en/settings): macOS = /Library/Application Support/ClaudeCode/managed-settings.json ; Linux/WSL = /etc/claude-code/managed-settings.json ; Windows = C:\\Program Files\\ClaudeCode\\managed-settings.json . A managed-settings.d/*.json drop-in dir also works (merged alphabetically). Managed settings are the HIGHEST-precedence scope: they override the same keys elsewhere (permission rules merge; managed-settings.d fragments merge). Test after placing: /model should not list fable and --model fable should be rejected; /doctor lists any stripped entries.",

  "//VALIDATION": "Managed settings parse TOLERANTLY: an entry that fails schema validation (like these //-comment keys, if the schema rejects them) is STRIPPED with a warning and every remaining valid setting still applies (docs/en/settings). So the comments are safe to leave in — expect /doctor to mention them. User/project/local settings are the opposite (strict, whole-file rejection): never paste //-keys into those.",

  "//CAUTION": "Because managed settings override per-key at highest precedence, put ONLY the keys you truly want to force here (this allowlist), not your whole config. Everything else stays in ~/.claude/settings.json.",

  "availableModels": ["opus", "sonnet", "haiku"],
  "enforceAvailableModels": true
}
