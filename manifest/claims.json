{
  "$schema_note": "Machine-readable baseline of every version-dependent claim the cost-control bundle relies on. The cost-control-verify skill diffs the CURRENT Claude Code version's docs against this file, writes CHANGELOG entries, and patches the 'affects' targets. verdict/confidence are as-of baseline_verified_date. Edit via the skill, not by hand, so the changelog stays honest.",
  "baseline_docs_version": "2.1.267 (pinned 2026-09-10 via `claude --version`; live docs track v2.1.267 this pass — binary and docs aligned, no POST-LOCK bracketing needed)",
  "baseline_verified_date": "2026-09-10",
  "baseline_scope_note": "PASS OF 2026-09-10: drift v2.1.266 -> v2.1.267 (single patch bump), verified against raw primary-source markdown (curl of 15 code.claude.com/docs/en/*.md pages + the changelog + local grep; established method, no subagents). ALL 16 EXISTING CLAIMS RE-MATCHED VERBATIM — no verdict changed, and the one CHANGED claim (subagent-model-precedence) was re-confirmed still in its v2.1.251 state, so the guard rewrite applied on 2026-09-08 remains correct. ONE CLAIM ADDED: max-effort-level, the v2.1.267 `maxEffortLevel` setting — the first bound on effort level, enforceable downward across scopes, and the only documented way to make ultracode unavailable (ultracode being the mode that ignores CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS). Other in-range deltas, all prose-only: v2.1.267 RETIRED TWO THIRDS OF THIS BUNDLE'S PROMPT-CACHE ADVICE — /model switches no longer re-send every tool definition, and mid-session MCP/plugin tool additions now arrive as deferred definitions instead of rewriting the tool block, so only mid-session CLAUDE.md edits still force a full uncached rebuild (subagents and --system-prompt sessions also record prompt+tool defs once now); `effort:` frontmatter on subagents/skills/commands was silently IGNORED on Opus 4.7 / Opus 4.8 / Fable 5 until v2.1.267 fixed it, so any pre-v2.1.267 effort pin was a no-op on those models; managed allowedHttpHookUrls / httpHookAllowedEnvVars / allowedChannelPlugins now admit nothing rather than everything when unreadable (fail-closed, same direction as this bundle); the usage-limit warning no longer flickers when different models report different limit windows (Claude Code UI, not the statusline payload); and Workflow agent() calls with large output schemas are no longer refused outright in auto mode. NO EXECUTABLE GUARDRAIL CHANGED THIS PASS.",
  "verify_sources": [
    "https://code.claude.com/docs/en/changelog.md",
    "https://code.claude.com/docs/en/hooks.md",
    "https://code.claude.com/docs/en/sub-agents.md",
    "https://code.claude.com/docs/en/tools-reference.md",
    "https://code.claude.com/docs/en/model-config.md",
    "https://code.claude.com/docs/en/statusline.md",
    "https://code.claude.com/docs/en/monitoring-usage.md",
    "https://code.claude.com/docs/en/settings.md",
    "https://code.claude.com/docs/en/settings-reference.md",
    "https://code.claude.com/docs/en/managed-settings.md",
    "https://code.claude.com/docs/en/agent-view.md",
    "https://code.claude.com/docs/en/workflows.md",
    "https://code.claude.com/docs/en/output-styles.md",
    "https://code.claude.com/docs/en/costs.md"
  ],
  "claims": [
    {
      "id": "spawn-gating-event",
      "summary": "Which hook surface can block a subagent spawn.",
      "verdict": "CONFIRMED",
      "confidence": "high",
      "current_value": "RE-VERIFIED 2026-09-10 vs v2.1.267 docs — UNCHANGED, fifth consecutive pass. PreToolUse row still VERBATIM 'PreToolUse | `hookSpecificOutput` | `permissionDecision` (allow/deny/ask/defer), `permissionDecisionReason`' (hooks.md:1022); SubagentStart still sits on the Context-only row ending 'No blocking or decision control' (hooks.md:1030); TaskCreated still 'When a task is being created via `TaskCreate`' (hooks.md:51), still unregistered here; PreModelSwitch still 'Before Claude Code applies a model switch that you or a client requested. Can block the switch' (hooks.md:65). Nothing in v2.1.267 touches the spawn surface. PRIOR PASS: RE-VERIFIED 2026-09-08 vs v2.1.266 docs — MECHANISM UNCHANGED a fourth time: PreToolUse row VERBATIM 'PreToolUse | `hookSpecificOutput` | `permissionDecision` (allow/deny/ask/defer), `permissionDecisionReason`'; SubagentStart still Context-only with 'No blocking or decision control'; the exit-2 wording and the SubagentStart-notice-in-its-own-transcript sentence both re-matched; TaskCreated still the task-list event, still unregistered here. Cosmetic table churn only: the Context-only row is now 'SessionStart, SubagentStart, PostModelSwitch' (Setup moved down to the 'None / no decision control' row — Setup was never used for blocking, no impact). ➕ NEW BLOCKING SURFACE IN RANGE (v2.1.251), adjacent to but distinct from spawn gating: hooks.md lifecycle table VERBATIM '`PreModelSwitch` | Before Claude Code applies a model switch that you or a client requested. Can block the switch' and '`PostModelSwitch` | After the session's model changes, including changes Claude Code makes on its own'; decision table VERBATIM 'PreModelSwitch | `hookSpecificOutput` or top-level `decision` | `permissionDecision` (allow/deny/ask), `permissionDecisionReason`. `decision: \"block\"` also cancels the switch'; exit-code table VERBATIM 'PreModelSwitch | Yes | Blocks the model switch and shows stderr to the user'. Matcher runs on a canonical name derived from `to_model`. TWO CAVEATS a future implementation must honor, VERBATIM: 'Claude Code doesn't run PreModelSwitch hooks for switches it makes on its own, such as an automatic model fallback or restoring the model when you resume a session' and 'When Claude Code can't determine a canonical name for the target... it runs every PreModelSwitch hook regardless of matcher. A hook that blocks should therefore check `to_model` from its input rather than rely on the matcher alone.' Also: a PreModelSwitch hook canceled at its timeout BLOCKS the switch (fail-closed, unlike PreToolUse's guards here), and its command-hook timeout default is lowered to 30s. This bundle does NOT register it — recorded as an available lever (documented in CLAUDE.snippet.md + session-topology + ADR evidence row 4 this pass), not as a gap in the existing gate. PRIOR PASS: RE-VERIFIED 2026-08-26(b) vs v2.1.246 docs — MECHANISM UNCHANGED a third time: PreToolUse row VERBATIM 'PreToolUse | hookSpecificOutput | permissionDecision (allow/deny/ask/defer), permissionDecisionReason'; Context-only row and exit-2 wording re-matched, exit-2 adds one sentence: 'For SubagentStart, the notice appears in the subagent's own transcript, not in the parent conversation.' TaskCreated row now phrased 'Exit code 2 or decision: \"block\" cancels the task and returns the message to Claude. continue: false is ignored' — still the task-list event, still unregistered here. New non-load-bearing PreToolUse capability: 'updatedInput directly under hookSpecificOutput replaces a tool's arguments before it runs' (the guards only deny; no change). PRIOR PASS: RE-VERIFIED 2026-08-26 vs v2.1.233 docs — MECHANISM UNCHANGED again: the PreToolUse decision row, the SubagentStart context-only row, the exit-code-2 wording, and the TaskCreated lifecycle row all re-matched (link markup aside). In-range note (changelog v2.1.233 VERBATIM): 'Todo/task-tracking tools (TaskCreate/Get/Update/List, TodoWrite) are no longer available on Opus 4.8, Sonnet 5, Fable 5, Mythos 5, and newer models; set CLAUDE_CODE_ENABLE_TODO_TOOLS=1 to bring them back' — so on this install's models TaskCreated effectively never fires by default; still zero wiring impact (nothing registers on it). PRIOR PASS: RE-VERIFIED 2026-08-09 vs v2.1.226 docs — MECHANISM AND WORDING BOTH UNCHANGED since the v2.1.212 pass; every quote below re-matched verbatim in the current hooks.md/sub-agents.md. A subagent spawn IS a PreToolUse tool call on the `Agent` tool. sub-agents.md VERBATIM: 'In version 2.1.63, the Task tool was renamed to Agent. Existing `Task(...)` references in settings and agent definitions still work as aliases.' PreToolUse is the ONLY hook surface that can block a spawn — hooks.md decision-control table VERBATIM: 'PreToolUse | hookSpecificOutput | permissionDecision (allow/deny/ask/defer), permissionDecisionReason' (NOTE: 'defer' is a 4th value not present at the 2026-07-16 baseline; the guards only ever emit 'deny', so no change needed). `SubagentStart` CANNOT block — the current wording is NOT the baseline's 'Shows stderr to user only' but hooks.md decision-control table VERBATIM: 'SessionStart, Setup, SubagentStart | Context only | hookSpecificOutput.additionalContext adds context for Claude. ... No blocking or decision control'; corroborated VERBATIM: 'For SessionStart, Setup, and SubagentStart, the exit code 2 stderr renders in the transcript as a <hook name> hook error notice ... Claude doesn't see it, and the session or subagent proceeds.' SubagentStart payload carries agent_id + agent_type only, no model (hooks.md 'SubagentStart input'). `TaskCreated` is the task-LIST event — hooks.md lifecycle table VERBATIM: 'TaskCreated | When a task is being created via TaskCreate' — unrelated to spawns; blocking it rolls back a todo item. (It also takes no matcher: 'TaskCreated ... don't support matchers and always fire on every occurrence'.)",
      "since_version": "2.1.63 (rename); 'defer' permissionDecision value present as of v2.1.212 docs",
      "doc_urls": [
        "https://code.claude.com/docs/en/hooks.md",
        "https://code.claude.com/docs/en/sub-agents.md",
        "https://code.claude.com/docs/en/tools-reference.md"
      ],
      "verify": "doc-fetch hooks.md exit-code table + sub-agents.md rename note; live: spawn fable subagent, expect deny entry in model-guard.jsonl",
      "affects": [
        {
          "file": "hooks/guard-subagent-model.sh",
          "what": "PreToolUse-only blocking; SubagentStart observe-only; TaskCreated passthrough"
        },
        {
          "file": "hooks/guard-usage-budget.sh",
          "what": "PreToolUse-only; Agent|Task = spawn"
        },
        {
          "file": "settings.snippet.json",
          "what": "guards on PreToolUse Agent|Task; SubagentStart/Stop logging only; NO TaskCreated registration"
        },
        {
          "file": "agents/*.md",
          "what": "frontmatter gate matcher Agent|Task"
        }
      ]
    },
    {
      "id": "spawn-tool-input-schema",
      "verdict": "CONFIRMED",
      "confidence": "high",
      "summary": "Field layout of the Agent tool's tool_input as seen by PreToolUse hooks.",
      "current_value": "RE-VERIFIED 2026-09-10 vs v2.1.267 docs — UNCHANGED; the Agent tool_input table is byte-for-byte the same four rows (`prompt`/`description`/`subagent_type`/`model`, hooks.md:1700-1703), so the guards' `.tool_input.model` and `.tool_input.subagent_type` probes stay correct. PRIOR PASS: RE-VERIFIED 2026-09-08 vs v2.1.266 docs — the four tool_input fields (prompt/description/subagent_type/model) are byte-for-byte unchanged AGAIN; the guards' probes stay correct. The PostToolUse tool_response table and the 'use the token and cost counters filtered to query_source \"subagent\"' rollup guidance also re-matched. In-range: v2.1.247 'Fixed sub-agents dying on a first-call model 404: they now use the session's fallback model chain, and the error returned to the parent includes the error type, status, request id, and model' — a 404 on a cheap model can now silently land the subagent on a FALLBACK model rather than failing, so resolvedModel/modelsUsed in PostToolUse is the only way to see what actually ran; v2.1.265 fixed resuming a foreground subagent changing its tool list and system prompt prefix (which broke prompt-cache reuse). PRIOR PASS: RE-VERIFIED 2026-08-26(b) vs v2.1.246 docs — the four tool_input fields (prompt/description/subagent_type/model) are byte-for-byte unchanged AGAIN; probes stay correct. The rollup guidance re-matched verbatim ('for token and cost rollups across subagents, use the token and cost counters filtered to query_source \"subagent\"'). In-range observability wins: v2.1.243 'Added the model (and effort level) each subagent ran on to /tasks and the agent detail dialogs' — mid-run model-swap detection no longer needs modelsUsed parsing for eyeball checks; v2.1.246 'a subagent that stops at its maxTurns limit now returns its output marked as partial, with a hint to continue it via SendMessage, instead of appearing finished'; v2.1.235 'Fixed the Agent tool advertising a general-purpose default in sessions where that agent is unavailable'. PRIOR PASS: RE-VERIFIED 2026-08-26 vs v2.1.233 docs — the four tool_input fields are AGAIN byte-for-byte unchanged (hooks.md '##### Agent' table re-matched); probes stay correct. TWO REFINEMENTS to the PostToolUse cost-attribution note: (1) hooks.md now says outright that the completed-response usage fields are NOT a whole-run rollup — VERBATIM: 'for token and cost rollups across subagents, use the token and cost counters filtered to query_source \"subagent\", since totalTokens and usage cover the final request only' — so the 2026-08-09 'documented surface for per-subagent cost attribution' framing overstated it; telemetry counters are the documented rollup path. (2) resolvedModel semantics tightened — VERBATIM: 'On a completed response, resolvedModel names the model the subagent STARTED on'; 'On an async_launched response, resolvedModel names the model in use when the agent moved to the background'. Also in-range (changelog v2.1.232): 'Subagent forking is now on by default ... and non-teammate agent spawns in interactive sessions now run in the background by default' — reinforcing that tool_response is usually async_launched with NO usage fields. PRIOR PASS: RE-VERIFIED 2026-08-09 vs v2.1.226 docs — the four tool_input fields are BYTE-FOR-BYTE UNCHANGED, so the guards' probes remain correct. One addition to the PostToolUse side since the last pass: `modelsUsed` (array, 'Models used in order, with consecutive repeats collapsed; set only when the model was swapped mid-run. Requires Claude Code v2.1.212 or later') — a way to detect a subagent that changed models mid-run, which resolvedModel alone would hide. Original v2.1.212 finding retained below. hooks.md 'Tool input schemas' section '##### Agent — Spawns a subagent' publishes the table VERBATIM: `prompt` (string), `description` (string), `subagent_type` (string, e.g. \"Explore\", 'Type of specialized agent to use'), `model` (string, e.g. \"sonnet\", 'Optional model alias to override the default'). This CONFIRMS the guards' primary probe paths .tool_input.model and .tool_input.subagent_type are correct; the .params.model/.opts.model fallbacks are now redundant but harmless. BONUS (PostToolUse): tool_response for a completed Agent call carries resolvedModel (v2.1.174+ — 'the model the subagent actually runs on, which can differ from the model value in tool_input'), totalTokens, totalDurationMs, totalToolUseCount, and a usage{} token breakdown — a documented surface for per-subagent cost attribution. NOTE: as of v2.1.198 subagents run in the BACKGROUND by default, so an omitted run_in_background yields status 'async_launched' and tool_response carries NO usage fields.",
      "since_version": "2.1.63 (tool); schema documented as of the v2.1.212 docs",
      "doc_urls": [
        "https://code.claude.com/docs/en/hooks.md",
        "https://code.claude.com/docs/en/agent-sdk/subagents.md",
        "https://code.claude.com/docs/en/sub-agents.md"
      ],
      "verify": "doc-fetch hooks.md '##### Agent' tool-input table (CONFIRMED 2026-07-17). Previously LOAD-BEARING-and-unverified: wrong paths => model='' => allow. Probes now match the published schema; re-check this table after each update. Backstops remain: CC_REQUIRE_EXPLICIT_MODEL=1 and/or the availableModels managed gate.",
      "affects": [
        {
          "file": "hooks/guard-subagent-model.sh",
          "what": "model/subagent_type probes; fails open"
        },
        {
          "file": "hooks/guard-usage-budget.sh",
          "what": "same probes"
        }
      ]
    },
    {
      "id": "agent-frontmatter-hooks",
      "summary": "Subagent frontmatter supports a `hooks:` key scoped to that agent's lifecycle.",
      "verdict": "CONFIRMED",
      "confidence": "high",
      "current_value": "RE-VERIFIED 2026-09-10 vs v2.1.267 docs — UNCHANGED; the `hooks` row still reads VERBATIM 'Lifecycle hooks scoped to this subagent. Ignored for plugin subagents' (sub-agents.md:301), and plugin subagents still 'don't support the `hooks`, `mcpServers`, or `permissionMode` frontmatter fields' (sub-agents.md:234). ➕ v2.1.267 FIXED A SILENT NO-OP IN THIS AREA, changelog VERBATIM: 'Fixed `effort:` frontmatter on custom commands, skills, and subagents being ignored on models whose default effort is still pinned (Opus 4.7, Opus 4.8, Fable 5)' — any `effort:` pin written before v2.1.267 did nothing on those models and only now takes effect. This bundle's agents/*.md carry no `effort:` at all; adding one is a frontmatter change, so it is PROPOSED as HITL this pass, not applied. PRIOR PASS: RE-VERIFIED 2026-09-08 vs v2.1.266 docs — the `hooks` row ('Lifecycle hooks scoped to this subagent. Ignored for plugin subagents') and the stricter-trust sentence both re-matched verbatim; still moot here (~/.claude/agents/ exempt). NEW COST-RELEVANT FRONTMATTER FIELD in range, sub-agents.md VERBATIM: '`experimental` | No | Map of experimental options. Set its `cacheTtl` key to `5m` or `1h` to choose the prompt cache lifetime for this subagent's requests... Claude Code ignores any other value, ignores `1h` while your Claude subscription is using usage credits, and reads the field only from subagent files. Requires Claude Code v2.1.248 or later', plus 'Write `cacheTtl` inside the `experimental` map, not at the top level of the frontmatter.' A long-lived reviewer/worker agent that is resumed repeatedly is the case where `1h` pays for itself; not applied to agents/*.md this pass (frontmatter change = HITL, and the benefit is workload-specific). Documented in CLAUDE.snippet.md + session-topology. PRIOR PASS: RE-VERIFIED 2026-08-26(b) vs v2.1.246 docs — field and trust rule both re-matched verbatim ('This is a stricter rule than the one for hooks in settings files: trusting a parent folder isn't enough, and a -p session doesn't count as trusted.'); still moot here (~/.claude/agents/ exempt). PRIOR PASS: RE-VERIFIED 2026-08-26 vs v2.1.233 docs — field unchanged ('`hooks` | No | [Lifecycle hooks](#define-hooks-for-subagents) scoped to this subagent. Ignored for [plugin subagents]', link markup added). Trust rule STRENGTHENED in wording, VERBATIM: 'This is a stricter rule than the one for hooks in settings files: trusting a parent folder isn't enough, and a -p session doesn't count as trusted.' Still moot for this install (~/.claude/agents/ exempt). PRIOR PASS: RE-VERIFIED 2026-08-09 vs v2.1.226 docs — field unchanged. sub-agents.md 'Supported frontmatter fields' VERBATIM: '`hooks` | No | Lifecycle hooks scoped to this subagent. Ignored for plugin subagents'. Plugin caveat unchanged. NEW GATING PRECONDITION as of v2.1.218 — VERBATIM: 'To let a project-level subagent's frontmatter hooks run, accept the workspace trust dialog for the folder that contains the agent file. Hooks from user-level subagents in `~/.claude/agents/` and from definitions you pass with `--agents` run without this step. If you added a folder with `--add-dir` from outside your trusted workspace's repository, trust that folder separately: its `.claude/agents/` hooks don't inherit the workspace's grant.' And the failure mode, VERBATIM: 'Until you trust the folder, the subagent still runs, but Claude Code skips its frontmatter hooks and logs an error to the debug log explaining how to trust the folder... Before v2.1.218, frontmatter hooks could run from folders you hadn't trusted, including in non-interactive sessions.' => THIS INSTALL IS UNAFFECTED (agents live in ~/.claude/agents/, explicitly exempt). It becomes load-bearing only if these agents are ever copied to a project's .claude/agents/ — there, an untrusted folder silently drops the frontmatter guard with no user-visible error outside the debug log.",
      "since_version": "unknown; project-level workspace-trust precondition added 2.1.218",
      "doc_urls": [
        "https://code.claude.com/docs/en/sub-agents.md"
      ],
      "verify": "doc-fetch sub-agents.md#supported-frontmatter-fields",
      "affects": [
        {
          "file": "agents/*.md",
          "what": "the nested-spawn gate mechanism"
        }
      ]
    },
    {
      "id": "nested-spawn-hooks",
      "summary": "Do settings.json PreToolUse hooks fire for tool calls made BY a subagent?",
      "verdict": "CONFIRMED",
      "confidence": "high",
      "current_value": "RE-VERIFIED 2026-09-10 vs v2.1.267 docs — UNCHANGED; both corroborating sentences re-matched verbatim (hooks.md:267 'Hooks from settings files, managed policy settings, and plugins also run inside [subagents]... tool events such as `PreToolUse` and `PostToolUse` fire the same configured hooks as in the main conversation, and the input carries the `agent_id` and `agent_type`'; sub-agents.md:709 'a `PreToolUse` hook in `settings.json` also runs before every tool a subagent uses'). PRIOR PASS: RE-VERIFIED 2026-09-08 vs v2.1.266 docs — both corroborating sentences re-matched verbatim (hooks.md 'Hooks from settings files, managed policy settings, and plugins also run inside subagents... fire the same configured hooks as in the main conversation'; sub-agents.md 'a `PreToolUse` hook in `settings.json` also runs before every tool a subagent uses'), depth-3 default and the fork-at-limit wording unchanged. PRIOR PASS: RE-VERIFIED 2026-08-26(b) vs v2.1.246 docs — both corroborating sentences re-matched verbatim (hooks.md 'also run inside [subagents]... fire the same configured hooks as in the main conversation'; sub-agents.md 'a PreToolUse hook in settings.json also runs before every tool a subagent uses'), depth-3 and fork-at-limit wording unchanged. PRIOR PASS: RE-VERIFIED 2026-08-26 vs v2.1.233 docs — the load-bearing sentence re-matched verbatim at hooks.md ('Hooks from settings files, managed policy settings, and plugins also run inside [subagents](/docs/en/sub-agents). When a subagent calls a tool, tool events such as PreToolUse and PostToolUse fire the same configured hooks as in the main conversation...'), now ALSO corroborated in sub-agents.md: 'a PreToolUse hook in settings.json also runs before every tool a subagent uses.' Depth default still 3; fork-at-limit behavior refined, VERBATIM: 'A fork at the limit keeps Agent in its inherited tool list, but the tool returns an error instead of spawning.' PRIOR PASS: RESOLVED 2026-08-09 vs v2.1.226 docs — PARTIAL→CONFIRMED. hooks.md now states it outright, VERBATIM: 'Hooks from settings files, managed policy settings, and plugins also run inside subagents. When a subagent calls a tool, tool events such as PreToolUse and PostToolUse fire the same configured hooks as in the main conversation, and the input carries the agent_id and agent_type common input fields that identify the subagent.' The ambiguity that made this PARTIAL at the two prior baselines is gone: the settings-level gate DOES cover subagent-originated tool calls, including nested spawns. Consequence for the bundle: the frontmatter-replicated guard in agents/*.md is now REDUNDANCY, not the sole nested gate — keep it, because it is still the ONLY thing that would cover a case the settings gate misses, and because plugin subagents cut the other way: sub-agents.md VERBATIM 'plugin subagents don't support the hooks, mcpServers, or permissionMode frontmatter fields', so plugin-shipped subagents are covered ONLY by the settings-level gate (now confirmed to fire). DEPTH LIMIT CHANGED — this is the material delta since v2.1.212: sub-agents.md VERBATIM 'By default, a subagent can spawn subagents of its own, up to three layers below the main conversation. At the depth limit, Claude Code withholds the Agent tool from every subagent except a fork' and 'To change the limit, set CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH to the number of subagent layers you want below your main conversation.' The prior baseline recorded 'depth five ... fixed and not configurable' — BOTH the number AND the configurability were superseded. Churn in range: v2.1.217 disabled nesting by default; v2.1.219 set the default to 3. NEW v2.1.218 TRUST CAVEAT (sub-agents.md VERBATIM): 'To let a project-level subagent's frontmatter hooks run, accept the workspace trust dialog for the folder that contains the agent file. Hooks from user-level subagents in ~/.claude/agents/ and from definitions you pass with --agents run without this step.' The bundle installs to ~/.claude/agents/ => EXEMPT, no action needed; a project-scoped copy of these agents would silently skip its frontmatter hooks in an untrusted folder ('the subagent still runs, but Claude Code skips its frontmatter hooks and logs an error to the debug log').",
      "since_version": "2.1.172 (nested spawns); 2.1.219 (depth default 3, configurable via CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH); 2.1.218 (project-level frontmatter-hook trust requirement); settings-hooks-fire-in-subagents documented as of the v2.1.226 docs",
      "doc_urls": [
        "https://code.claude.com/docs/en/hooks.md",
        "https://code.claude.com/docs/en/sub-agents.md"
      ],
      "verify": "doc-fetch hooks.md 'Hooks from settings files ... also run inside subagents' (CONFIRMED 2026-08-09) + sub-agents.md#let-subagents-spawn-their-own-subagents for the depth default",
      "affects": [
        {
          "file": "agents/*.md",
          "what": "gate replicated into frontmatter — now redundancy, since settings hooks are confirmed to fire inside subagents; keep (zero cost, covers plugin-agent asymmetry in reverse)"
        },
        {
          "file": "CLAUDE.snippet.md",
          "what": "spawn-gate paragraph: ambiguity language removed; depth/trust caveats added"
        },
        {
          "file": "README.md",
          "what": "nested-spawn rationale"
        },
        {
          "file": "ADR-claude-code-cost-control.md",
          "what": "evidence row 11 + 12"
        }
      ]
    },
    {
      "id": "subagent-model-precedence",
      "summary": "CLAUDE_CODE_SUBAGENT_MODEL precedence. AS OF v2.1.251 it is a DEFAULT, not an override (reversed); =inherit == unset; CLAUDE_CODE_SUBAGENT_MODEL_FORCE=1 (v2.1.257+) carries the old override semantics.",
      "verdict": "CHANGED",
      "confidence": "high",
      "current_value": "RE-CONFIRMED CHANGED 2026-09-10 vs v2.1.267 docs — the v2.1.251 order is STILL what ships; no second reversal. model-config.md:780 still reads VERBATIM 'A per-invocation model or a definition's `model` field, including `inherit`, takes precedence'; sub-agents.md:356 still carries the retraction 'Before v2.1.251, `CLAUDE_CODE_SUBAGENT_MODEL` came first in this order and overrode both the per-invocation parameter and the frontmatter, including `model: inherit`'; :375 still requires v2.1.257+ for `CLAUDE_CODE_SUBAGENT_MODEL_FORCE`; :378 'If you set only `CLAUDE_CODE_SUBAGENT_MODEL_FORCE`, subagents run on the main conversation's model'; :393 it 'ignores the `model` field of every subagent definition, including the built-in Explore and Plan subagents'. The guard rewrite applied last pass therefore remains correct on this build — no further patch. PRIOR PASS: ⚠️ CHANGED 2026-09-08 vs v2.1.266 docs — THE PRECEDENCE REVERSED IN v2.1.251, invalidating the core premise this bundle was designed around. sub-agents.md#choose-a-model now resolves, VERBATIM as a numbered list: '1. The per-invocation `model` parameter / 2. The subagent definition's `model` frontmatter, where `inherit` selects the main conversation's model / 3. The `CLAUDE_CODE_SUBAGENT_MODEL` environment variable, when you set it to a model alias or model ID / 4. The main conversation's model', followed by the explicit retraction, VERBATIM: 'Before v2.1.251, `CLAUDE_CODE_SUBAGENT_MODEL` came first in this order and overrode both the per-invocation parameter and the frontmatter, including `model: inherit`.' model-config.md's env-var row was rewritten to match, VERBATIM: 'The default model for subagents, agent team teammates, and workflow agents that aren't assigned a model another way. Accepts an alias such as `haiku` or a full model name. A per-invocation model or a definition's `model` field, including `inherit`, takes precedence.' THE OVERRIDE SEMANTICS MOVED, not disappeared — sub-agents.md#run-every-subagent-on-one-model VERBATIM: '`CLAUDE_CODE_SUBAGENT_MODEL` is a default, so a subagent's definition or a model Claude passes still takes precedence over it. To apply one model to every subagent, teammate, and workflow agent, also set `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` to `1`. Requires Claude Code v2.1.257 or later.' Its semantics, VERBATIM: 'If you set both variables, subagents run on the model in `CLAUDE_CODE_SUBAGENT_MODEL`. If you set only `CLAUDE_CODE_SUBAGENT_MODEL_FORCE`, subagents run on the main conversation's model.' and 'While `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` is on, Claude Code ignores the `model` field of every subagent definition, including the built-in Explore and Plan subagents, and Claude can't pass a model when it starts a subagent.' Two exceptions stay on the main model regardless: a fork, and a skill run in a subagent with `model: inherit`. Also VERBATIM: 'Setting `CLAUDE_CODE_SUBAGENT_MODEL` by itself doesn't change the model the built-in Explore and Plan subagents run on.' => TWO CONSEQUENCES. (1) DOC/PROSE: the bundle's repeated claim that there is 'no soft default knob' is now false; a global CLAUDE_CODE_SUBAGENT_MODEL=haiku is safe and roster-preserving. Patched this pass in CLAUDE.snippet.md, settings.snippet.json, ADR (options table + evidence row 1), README, session-topology. (2) EXECUTABLE — guard-subagent-model.sh:91 and guard-usage-budget.sh:88 unconditionally overwrite the parsed model with the env var, mirroring the OLD precedence. On v2.1.251+ that mis-resolves in both directions: env=haiku + an explicit `model: fable` spawn => fable actually runs but the guard evaluates haiku and ALLOWS (the gate fails open on exactly what it exists to block); env=fable + an explicit `model: haiku` spawn => haiku actually runs but the guard DENIES. Correct logic is: explicit tool_input.model wins; the env var applies only when no explicit model was passed (frontmatter, invisible to the hook, sits between them); and CLAUDE_CODE_SUBAGENT_MODEL_FORCE=1 restores the old unconditional override. HITL patch PROPOSED 2026-09-08, CONFIRMED by the user, and APPLIED the same pass (both guards + tests/test-hooks.sh; suite green at 61/11/14/4 PASS, 0 FAIL). Residual known limit, documented in the hook comment: in the no-explicit-model branch frontmatter still outranks the env var and the hook cannot see frontmatter, so a deny-wrong is possible only if CLAUDE_CODE_SUBAGENT_MODEL itself names a blocked model — which policy forbids. Blast radius today is ZERO on this install: CLAUDE_CODE_SUBAGENT_MODEL is unset in ~/.claude/settings.json and in the live environment, so line 91/88 is a no-op and the guard reads tool_input.model exactly as it should. The bug is latent, and turns live the moment the (now newly-recommended) global default is set. Note the tests encode the old behavior too (tests/test-hooks.sh:82 asserts a deny when env=fable overrides an explicit haiku).",
      "since_version": "2.1.196 (inherit==unset); 2.1.211 (per-invocation model survives resume); 2.1.251 (PRECEDENCE REVERSED: env var demoted to a default); 2.1.257 (CLAUDE_CODE_SUBAGENT_MODEL_FORCE added)",
      "doc_urls": [
        "https://code.claude.com/docs/en/model-config.md",
        "https://code.claude.com/docs/en/sub-agents.md"
      ],
      "verify": "doc-fetch sub-agents.md#choose-a-model resolution list + #run-every-subagent-on-one-model, and model-config.md environment-variables row. The load-bearing sentence to re-match is the 'Before v2.1.251...' note; if it ever disappears, re-read the numbered resolution list rather than trusting this entry.",
      "affects": [
        {
          "file": "hooks/guard-subagent-model.sh",
          "what": "model resolution rewritten 2026-09-08 to the v2.1.251 order: explicit tool_input.model wins; env var applies only when no explicit model was passed; CLAUDE_CODE_SUBAGENT_MODEL_FORCE=1 restores the old unconditional override. APPLIED (HITL confirmed)"
        },
        {
          "file": "hooks/guard-usage-budget.sh",
          "what": "same resolution block, kept in sync. APPLIED (HITL confirmed)"
        },
        {
          "file": "tests/test-hooks.sh",
          "what": "old 'env override denies despite explicit haiku' assertion replaced by five cases: env-does-not-override-explicit, env-as-default-when-no-model, _FORCE=1 overrides, _FORCE=0 is off, inherit==unset. APPLIED"
        },
        {
          "file": "CLAUDE.snippet.md",
          "what": "precedence paragraph rewritten this pass"
        },
        {
          "file": "settings.snippet.json",
          "what": "//subagentModelEnv comment rewritten this pass"
        },
        {
          "file": "ADR-claude-code-cost-control.md",
          "what": "options table rows + evidence row 1 rewritten this pass"
        },
        {
          "file": "README.md",
          "what": "backstop paragraph amended this pass"
        },
        {
          "file": "session-topology-and-controls.md",
          "what": "two new control rows added this pass"
        },
        {
          "file": "~/.claude/CLAUDE.md",
          "what": "live global instructions synced from CLAUDE.snippet.md this pass"
        }
      ]
    },
    {
      "id": "explore-agent-model",
      "verdict": "CONFIRMED",
      "confidence": "high",
      "summary": "Built-in Explore agent's default model (research said it stopped forcing Haiku ~2.1.198).",
      "current_value": "RE-VERIFIED 2026-09-10 vs v2.1.267 docs — UNCHANGED; sub-agents.md:39 still reads 'inherits from the main conversation, capped at Opus on the Claude API, so Explore never runs on a more expensive model than the one you already chose for the session, unless you set `CLAUDE_CODE_SUBAGENT_MODEL` and force it onto every subagent', and :398 'When you set only `CLAUDE_CODE_SUBAGENT_MODEL_FORCE`, the built-in Explore subagent keeps its model cap'. agents/explore.md (model: haiku) stays load-bearing. PRIOR PASS: RE-VERIFIED 2026-09-08 vs v2.1.266 docs — UNCHANGED; agents/explore.md (model: haiku) stays load-bearing. ONE NEW INTERACTION: sub-agents.md now qualifies the built-in Explore model line, VERBATIM: 'inherits from the main conversation, capped at Opus on the Claude API... unless you set `CLAUDE_CODE_SUBAGENT_MODEL` and force it onto every subagent' — i.e. CLAUDE_CODE_SUBAGENT_MODEL alone does NOT move built-in Explore/Plan; only CLAUDE_CODE_SUBAGENT_MODEL_FORCE=1 does, and with FORCE set alone Explore keeps its model cap. PRIOR PASS: RE-VERIFIED 2026-08-26(b) vs v2.1.246 docs — UNCHANGED; both key sentences re-matched verbatim again, agents/explore.md stays load-bearing. PRIOR PASS: RE-VERIFIED 2026-08-26 vs v2.1.233 docs — UNCHANGED; both key sentences re-matched verbatim ('As of v2.1.198, Explore inherits the main conversation's model...' / 'define one with model: haiku to keep exploration on a lower-cost model'), plus an added provider note: 'On any other provider, such as Amazon Bedrock..., Explore inherits the main conversation's model directly' (no Opus cap off-API — irrelevant here, this install is on the Claude API). agents/explore.md stays load-bearing. PRIOR PASS: RE-VERIFIED 2026-08-09 vs v2.1.226 docs — UNCHANGED; agents/explore.md (model: haiku) remains the documented remedy and stays load-bearing. Also new in range: CLAUDE_CODE_DISABLE_EXPLORE_PLAN_AGENTS=1 (v2.1.198+) removes the built-in Explore/Plan agents entirely if you prefer Claude read files directly. CONFIRMED 2026-07-17 vs v2.1.212 docs (was UNVERIFIED/low at the 2026-07-16 baseline; the 2.1.198 research figure is now doc-backed). sub-agents.md 'Built-in subagents' VERBATIM: 'As of v2.1.198, Explore inherits the main conversation's model instead of always running on Haiku. On the Claude API, the inherited model is capped at Opus: a main conversation on a higher tier runs Explore on Opus, and a main conversation on Sonnet or Haiku runs Explore on that same model.' And VERBATIM: 'A user or project subagent named `Explore` overrides the built-in and keeps its own `model` field, so define one with `model: haiku` to keep exploration on a lower-cost model.' => the bundle's agents/explore.md (model: haiku) is EXACTLY the documented remedy, and is load-bearing on v2.1.212: without it, Explore would inherit this user's Opus/Fable lead session and explore at lead-model cost.",
      "since_version": "2.1.198",
      "doc_urls": [
        "https://code.claude.com/docs/en/sub-agents.md"
      ],
      "verify": "doc-fetch sub-agents.md built-in subagents section (CONFIRMED 2026-07-17)",
      "affects": [
        {
          "file": "agents/explore.md",
          "what": "custom haiku override; harmless if built-in differs"
        }
      ]
    },
    {
      "id": "statusline-rate-limits",
      "summary": "Statusline stdin JSON carries live rate_limits + cost + context fields.",
      "verdict": "CONFIRMED",
      "confidence": "high",
      "current_value": "RE-VERIFIED 2026-09-10 vs v2.1.267 docs — SCHEMA UNCHANGED; `rate_limits.{five_hour,seven_day}.used_percentage` + `.resets_at`, the gateway-only `spend_limit` pair, and the `prompt_cache` object all re-matched (statusline.md:193-196), as did the Pro/Max-plus-first-response presence rule (:339) and the drop-on-reset behavior (:149). ⚠️ v2.1.267 fixed a DISPLAY-side flap that could otherwise be mistaken for sensor noise: 'Fixed the usage-limit warning flickering on and off during a session when requests for different models or modes report different limit windows'. That is Claude Code's own warning UI, not the statusline payload this bundle reads — no patch. PRIOR PASS: 🐞 LIVE SELF-TEST FOUND A REAL BUG THIS PASS (2026-09-08), now fixed: with `rate_limits` absent, statusline/usage-statusline.sh wrote a ZERO-BYTE state file. Cause was jq, not Claude Code — each value was written `$fp|select(.!=\"\")|tonumber? // null`, so an empty $fp made `select` yield nothing, the pipeline was already empty when `//` was reached, and jq emitted zero results for the WHOLE object; the atomic mv then installed an empty file, losing context_pct, model and updated_at as well. Fixed by parenthesizing each pipeline before the alternative: `(($fp|select(.!=\"\")|tonumber?) // null)`. Two regression tests added (absent window -> valid JSON with other fields intact; guard still fails open on a null pct). This was latent for the bundle's whole life but became ROUTINE at v2.1.266, which newly documents that a window is dropped once its resets_at passes — the live ~/.claude/.usage-state.json was found 0 bytes at the start of this pass, which is how it surfaced. Failure direction was always fail-open (guards exit 0 on unreadable state), so no over-throttling occurred; the cost was silently losing all guard state after every window reset. RE-VERIFIED + EXTENDED 2026-09-08 vs v2.1.266 docs — every field the scripts read is unchanged (used_percentage/resets_at rows, the '// empty' guard, the tput-cols capture note). THREE ADDITIONS in range. (1) rate_limits gained a third window, statusline.md VERBATIM: '`rate_limits.spend_limit.used_percentage`, `rate_limits.spend_limit.resets_at` | Behind a Claude apps gateway, the percentage used of the spend limit that applies to you, and the Unix epoch seconds when its period resets. The percentage runs from 0 to 100, or above 100 once you exceed the limit. Requires Claude Code v2.1.251 or later' — N/A on this personal install (no gateway), but note a >100 value is legal there, so any future max()/clamp must not assume 0-100. (2) ⚠️ THE PRESENCE RULE TIGHTENED, VERBATIM: 'Each window (`five_hour`, `seven_day`, `spend_limit`) may be independently absent, and Claude Code drops a window once its `resets_at` time passes.' The prior baseline recorded only 'may be independently absent'. So the post-reset state is ABSENCE, not zero — the scripts' `// empty` guard and their stale-state MAX_AGE check are what keep throttle.sh/guard-usage-budget.sh from reading a vanished window as 0% or as a stale high %; both already fail open. No patch needed, but this is now the documented reason the guard must never treat 'missing' as 'fine'. (3) NEW OBSERVABILITY SURFACE — a whole `prompt_cache` object, VERBATIM: 'The session's prompt cache statistics for the main conversation: hit ratio, misses, and whether the cache is warm... Absent until the main conversation's first API response. Requires Claude Code v2.1.251 or later', with fields warm, caching_observed, ttl, expires_at, requests, misses, expected_rebuilds, hit_ratio, cache_write_tokens, miss_recache_tokens, last_miss_at, recache_tokens_if_cold, plus last_miss_cause and miss_causes (v2.1.260+). Scope caveat VERBATIM: 'Claude Code doesn't count subagent requests in these statistics.' This is a candidate ENHANCEMENT for statusline/usage-statusline.sh (a cold-cache or high-miss indicator), not a correction — the bundle currently ignores the field. Mirrored in /cost as the 'Prompt cache (main)' line (costs.md). Also in range: v2.1.260 added a likely-cause diagnosis for misses. PRIOR PASS: RE-VERIFIED 2026-08-26(b) vs v2.1.246 docs — every field re-matched verbatim again (used_percentage/resets_at rows, the presence rule with '// empty', the tput-cols capture note). ✅ BUG CLEARED ON THIS BUILD: the stale-%-after-idle-reset bug the prior pass flagged is FIXED in-range (v2.1.243 changelog VERBATIM: 'Fixed the status line rate_limits fields and /usage still showing a rate-limit window's pre-reset usage percentage after the window reset while the session was idle') — the brief post-reset over-throttle window is gone. Also in-range: v2.1.246 'Fixed the status line's cost and duration resetting to zero after navigating to the agents view and back'; promptCacheTtl/subagentPromptCacheTtl are now REAL documented keys (settings-reference.md, 'Requires Claude Code v2.1.242 or later', scope Any file, values 5m/1h — mainly useful off-plan); modelPricing (managed per-model rates feeding /cost + statusline + telemetry cost) is changelog-only so far, not yet in settings-reference — managed orgs only, N/A here; v2.1.239 cost estimates include the 1.1x US-only-inference premium for data-residency workspaces (N/A personal install). PRIOR PASS: RE-VERIFIED 2026-08-26 vs v2.1.233 docs — every field re-matched verbatim; stale-% bug present on that build (see above, now moot). PRIOR PASS: RE-VERIFIED 2026-08-09 vs v2.1.226 docs — every field the statusline reads is UNCHANGED, and the live check confirms it end-to-end: ~/.claude/.usage-state.json holds a numeric five_hour_pct, so rate_limits is arriving. One clarification added in range: effort.level 'Reflects the live session value, including mid-session /effort changes. Ultracode is not a distinct level and reports as xhigh' — so a statusline cannot distinguish an ultracode session (which is exempt from the concurrency cap and the Large-workflow warning) from a plain xhigh one. statusline.md 'Available data': 'rate_limits.five_hour.used_percentage, rate_limits.seven_day.used_percentage | Percentage of the 5-hour or 7-day rate limit consumed, from 0 to 100' and 'rate_limits.five_hour.resets_at, rate_limits.seven_day.resets_at | Unix epoch seconds when the 5-hour or 7-day rate limit window resets'; plus cost.total_cost_usd; context_window.used_percentage; exceeds_200k_tokens; effort.level; output_style.name; agent.name. Presence rule VERBATIM: 'rate_limits: appears only for Claude.ai subscribers (Pro/Max) after the first API response in the session. Each window (five_hour, seven_day) may be independently absent. Use jq -r '.rate_limits.five_hour.used_percentage // empty' to handle absence gracefully.' — exactly the guard the scripts use. Colors VERBATIM: 'Colors: use ANSI escape codes like \\033[32m for green (terminal must support them)'; capture VERBATIM: 'Claude Code captures your script's output instead of connecting it directly to the terminal, so tput cols and language-level width detection cannot read the terminal size from inside the script' => stdout is never a tty, so never gate colors on isatty (the scripts gate on CC_STATUSLINE_NOCOLOR only). Related v2.1.153+: COLUMNS/LINES env vars carry terminal dimensions if ever needed.",
      "since_version": "unknown",
      "doc_urls": [
        "https://code.claude.com/docs/en/statusline.md"
      ],
      "verify": "doc-fetch statusline.md; or write hook/statusline stdin to a file mid-session",
      "affects": [
        {
          "file": "statusline/usage-statusline.sh",
          "what": "reads rate_limits.*; colors gated on CC_STATUSLINE_NOCOLOR only; state-file jq parenthesization FIXED 2026-09-08 (absent window no longer yields a 0-byte state file). prompt_cache object (v2.1.251) available but not yet consumed"
        },
        {
          "file": "hooks/throttle.sh",
          "what": "reads state file derived from them; unaffected by the fix (already fails open on missing/null)"
        },
        {
          "file": "hooks/guard-usage-budget.sh",
          "what": "reads five_hour_pct from state file; unaffected by the fix (already fails open)"
        },
        {
          "file": "tests/test-hooks.sh",
          "what": "two regression tests added 2026-09-08 for the absent-window state file"
        }
      ]
    },
    {
      "id": "workflow-size-config",
      "summary": "Dynamic workflow size options + runtime caps + large-workflow warning.",
      "verdict": "CONFIRMED",
      "confidence": "high",
      "current_value": "RE-VERIFIED 2026-09-10 vs v2.1.267 docs — UNCHANGED; `workflowSizeGuideline` still documented at settings-reference.md:3973 with the sizes, the `medium` default, and the threshold-tracks-guideline rule intact. In range: v2.1.267 'Fixed Workflow `agent()` calls with large output schemas being refused in auto mode instead of being checked by the safety classifier' — removes a spurious auto-mode refusal, no cost impact. PRIOR PASS: RE-VERIFIED 2026-09-08 vs v2.1.266 docs — sizes, the medium default, the 25-agent/1.5M warning, the threshold-tracks-guideline sentence, and the 16-concurrent runtime cap all re-matched verbatim; this install's `medium` still warns at >15. In-range reliability fixes that stop wasted re-spend: v2.1.266 'Fixed resuming a workflow run after its container restarted; a resume whose run journal is missing now fails with a clear error instead of rerunning every agent'; v2.1.260 'Fixed Workflow tool subagents being restarted as stalled while a long context compaction was still in progress' and 'Workflow `agent({schema})` rejects a JSON Schema that can never be satisfied up front' (kills retry-loop burn); v2.1.259 'Fixed resuming a workflow run while its previous stopped run was still exiting, which could run duplicate copies of its agents'. Prompt-footprint win: v2.1.248 cut the Workflow tool description from ~5.7k to ~1k tokens by moving the reference into a bundled `workflow-authoring` skill. PRIOR PASS: RE-VERIFIED 2026-08-26(b) vs v2.1.246 docs — sizes, medium default ('The default is medium. Until you choose a value, the /config row shows medium (default)...'), the 25-agent/1.5M warning, AND the threshold-tracks-guideline sentence ('If you choose a size guideline yourself, its agent count replaces the 25-agent threshold') all re-matched verbatim — this install's medium still warns at >15. Concurrency row lightly reworded, VERBATIM: 'Up to 16 concurrent agents, fewer when Claude Code has fewer CPUs available, including inside a CPU-limited container' (same semantics). In-range cost-relevant fix, v2.1.246 changelog VERBATIM: 'Fixed pressing ← or running /background during a dynamic workflow restarting its finished subagents; it now asks first and says how many subagents would restart' — backgrounding a session mid-workflow no longer silently re-spends finished agents. PRIOR PASS: RE-VERIFIED 2026-08-26 vs v2.1.233 docs — sizes, runtime caps, medium default, and the warning all re-matched. TWO DELTAS: (1) ⚠️ THE WARNING THRESHOLD NOW TRACKS YOUR CHOSEN GUIDELINE — workflows.md VERBATIM: 'If you choose a size guideline yourself, its agent count replaces the 25-agent threshold. The built-in default guideline leaves the threshold at 25.' => with this install's explicit medium (<15), the Large-workflow warning fires at >15 scheduled agents, not >25 (the 1.5M projected-token trigger is unchanged; ultracode sessions still see no warning). (2) DOCS RESTRUCTURE — the workflowSizeGuideline key reference moved settings.md → settings-reference.md#workflowsizeguideline; also settable as '/config workflowSizeGuideline=small'. Cost-relevant in-range bonus (changelog v2.1.229): 'Improved workflow fan-outs to stagger same-prefix sibling agents so subsequent agents read the cached prompt prefix instead of re-paying it (CLAUDE_CODE_WORKFLOW_PREFIX_STAGGER_MS=0 disables)' — workflow fan-outs now partially reuse prompt cache. PRIOR PASS: UPGRADED 2026-08-09 vs v2.1.226 docs — PARTIAL→CONFIRMED; the last unverified piece is now published. Sizes unchanged (workflows.md table): unrestricted / small '<5 agents' / medium '<15' / large '<50'. Runtime caps unchanged: 'Up to 16 concurrent agents, fewer on machines with limited CPU cores'; 1000 total. The Large-workflow warning that the 2026-07-16 baseline flagged as 'NOT in docs / likely internal heuristic' is NOW DOCUMENTED, VERBATIM: 'When a workflow schedules more than 25 agents, or its projected token total passes 1.5 million, its progress line in the task panel below the input box shows a Large workflow warning. The warning points you to /workflows, where you can stop the run. Requires Claude Code v2.1.203 or later.' — the numbers the bundle recorded from research were exactly right. IMPORTANT COST CAVEAT, VERBATIM: 'Sessions with ultracode on don't show the warning, because turning ultracode on already opts you in to large runs.' TWO CHANGES SINCE BASELINE: (1) default is now medium, VERBATIM 'The default is medium. Until you choose a value, the /config row shows medium (default)... Requires Claude Code v2.1.219 or later; earlier versions default to unrestricted.' (2) it is NO LONGER /config-only — settings.md VERBATIM: 'workflowSizeGuideline | Default: medium. Sets the agent count Claude aims for in the dynamic workflows it writes. Claude Code sends the value to Claude as advice, not an enforced cap. Accepts unrestricted, small, medium, or large. Takes precedence over the Dynamic workflow size choice in /config, and Claude Code hides that row while a settings file sets the key. Requires Claude Code v2.1.219 or later'. The bundle's settings.snippet.json said 'Not a settings.json key; set it in-app' — that statement is now FALSE and was corrected this pass.",
      "since_version": "2.1.202 (sizes); 2.1.203 (Large-workflow warning); 2.1.219 (workflowSizeGuideline settings key + medium default)",
      "doc_urls": [
        "https://code.claude.com/docs/en/workflows.md",
        "https://code.claude.com/docs/en/settings-reference.md"
      ],
      "verify": "doc-fetch workflows.md#set-a-size-guideline + settings-reference.md#workflowsizeguideline (moved off settings.md in the v2.1.233-range docs restructure)",
      "affects": [
        {
          "file": "CLAUDE.snippet.md",
          "what": "workflow-size guidance"
        },
        {
          "file": "session-topology-and-controls.md",
          "what": "warning threshold + settings key"
        },
        {
          "file": "settings.snippet.json",
          "what": "the 'not a settings key' comment — corrected"
        },
        {
          "file": "ADR-claude-code-cost-control.md",
          "what": "evidence row 3"
        }
      ]
    },
    {
      "id": "telemetry-metrics",
      "summary": "Telemetry metrics + per-subagent attribution.",
      "verdict": "CONFIRMED",
      "confidence": "high",
      "current_value": "RE-VERIFIED 2026-09-10 vs v2.1.267 docs — UNCHANGED; the redaction rule still reads VERBATIM 'Built-in agent names and agents from official-marketplace plugins appear verbatim. Other user-defined agent names are replaced with `\"custom\"`' (monitoring-usage.md:546) and query_source is still 'One of `\"main\"`, `\"subagent\"`, or `\"auxiliary\"`' (:543). This bundle's worker/reviewer agents still collapse into one 'custom' series. PRIOR PASS: RE-VERIFIED 2026-09-08 vs v2.1.266 docs — the 3-value query_source list and the agent.name redaction rule ('Other user-defined agent names are replaced with \"custom\"') both re-matched verbatim, so this bundle's worker/reviewer agents still collapse into one 'custom' series and model-level grouping stays the per-role proxy. In-range: v2.1.259 fixed cloud-session metrics missing user.email/organization.id/user.account_uuid; v2.1.265 added user.email + user.groups to Desktop/Cowork gateway telemetry. Neither affects this local Prometheus dashboard. PRIOR PASS: RE-VERIFIED 2026-08-26(b) vs v2.1.246 docs — query_source 3-value list and the agent.name redaction rule both re-matched verbatim ('Other user-defined agent names are replaced with \"custom\"'). In-range: v2.1.239 'Fixed OpenTelemetry trace fragmentation: tool executions deferred by a PreToolUse hook now resume in the original turn's trace instead of starting a new trace' (dashboard traces defragment); v2.1.246 'Fixed telemetry and metrics requests to Anthropic carrying the API key configured for a third-party gateway'. PRIOR PASS: RE-VERIFIED 2026-08-09 vs v2.1.226 docs — env vars and metric names unchanged (CLAUDE_CODE_ENABLE_TELEMETRY=1; CLAUDE_CODE_ENHANCED_TELEMETRY_BETA=1; claude_code.token.usage / .cost.usage / .session.count). TWO CORRECTIONS to the baseline's 'BONUS: per-subagent attribution' note. (1) query_source has THREE values, not two — VERBATIM: 'query_source: Category of the subsystem that issued the request. One of \"main\", \"subagent\", or \"auxiliary\"'. (2) MATERIAL — agent.name is REDACTED for user-defined agents, VERBATIM: 'agent.name: Subagent type that issued the request. Built-in agent names and agents from official-marketplace plugins appear verbatim. Other user-defined agent names are replaced with \"custom\". Absent when the request was not issued by a named subagent type.' => the bundle's own worker/reviewer agents collapse into one 'custom' series in metrics; per-agent metric breakdown is NOT achievable for them. Workarounds: traces (CLAUDE_CODE_ENHANCED_TELEMETRY_BETA=1) or the PostToolUse tool_response cost fields (see spawn-tool-input-schema). NEW in range: CLAUDE_CODE_OTEL_CONTENT_MAX_LENGTH (v2.1.214+, default 61440 UTF-16 units / 60KB) to cut telemetry volume; message.uuid / client_request_id / tool_source log attrs (v2.1.214); OTEL_METRICS_EXPORTER=prometheus '# UNIT' line bug fixed in v2.1.216 (relevant to this bundle's Prometheus-based dashboard). NEGATIVE verified 2026-08-09 vs v2.1.226: OTEL_LOG_TOOL_DETAILS=1 does NOT un-redact the token counter's agent.name — its documented scope covers workflow.name, tool-span attributes (incl. subagent_type), and log-event fields only; the agent.name definition on the Cost counter carries no gate. Model-level grouping (attribute 'model', never redacted) is the working per-role proxy under this bundle's routing rubric.",
      "since_version": "unknown; agent.name redaction + 3-value query_source per v2.1.226 docs; CLAUDE_CODE_OTEL_CONTENT_MAX_LENGTH 2.1.214",
      "doc_urls": [
        "https://code.claude.com/docs/en/monitoring-usage.md"
      ],
      "verify": "doc-fetch monitoring-usage.md cost-counter + token-counter attribute lists",
      "affects": [
        {
          "file": "dashboard/grafana/dashboards/claude-usage.json",
          "what": "query_source breakdown works; an agent.name panel will show one 'custom' bucket for this bundle's agents"
        },
        {
          "file": "dashboard/README.md",
          "what": "subagent drill-down via metric attrs — corrected with the agent.name redaction caveat"
        }
      ]
    },
    {
      "id": "available-models-gate",
      "summary": "availableModels hard gate + managed-settings paths + enforcement scope.",
      "verdict": "CONFIRMED",
      "confidence": "high",
      "current_value": "RE-VERIFIED 2026-09-10 vs v2.1.267 docs — UNCHANGED; the three per-OS managed paths (managed-settings.md:40-41,108), the managed-settings.d merge, and the v2.1.259 refuse-to-start sentence (:295) all re-matched. ➕ v2.1.267 extended managed fail-closed behavior to three more keys, changelog VERBATIM: 'Fixed managed `allowedHttpHookUrls`, `httpHookAllowedEnvVars` and `allowedChannelPlugins` to admit nothing, not everything, when unreadable' — same direction as this bundle's design; no patch. PRIOR PASS: RE-VERIFIED 2026-09-08 vs v2.1.266 docs — the allowlist scope list, family-alias substitution, the three per-OS managed paths + legacy-ProgramData exclusion + managed-settings.d merge, and the auto-mode classifier caveat all re-matched verbatim. ⚠️ ONE CORRECTION, v2.1.259: a managed source that cannot be PARSED no longer degrades quietly. managed-settings.md VERBATIM: 'When a managed settings file, drop-in file, MDM plist, or HKLM registry value is present but can't be parsed as a JSON object, Claude Code refuses to start and prints an error naming the source, even when another admin source delivers a valid policy', and for a policyHelper, VERBATIM: 'any schema violation that survives fails the whole helper run, and at startup Claude Code refuses to start, the same as for a helper that exits non-zero.' ENTRY-level tolerance is unchanged (bad entries repaired/stripped with a warning, the rest applies), so the //-comment keys in managed-settings.snippet.json remain safe — but a malformed FILE now bricks every session on the machine instead of silently un-enforcing the gate. managed-settings.snippet.json's //VALIDATION note rewritten this pass to say: validate the JSON before placing it. Adjacent, from settings.md: 'Unless a managed settings document can't be parsed, Claude Code skips the broken file or values and continues with the rest' in -p runs. New in range and relevant if a version-pinned allowlist is ever placed, model-config.md VERBATIM: 'A version prefix also matches later model IDs that extend it with another segment, so `claude-fable-5` permits both Fable 5 and Fable 5.1, while `claude-fable-5-1` permits Fable 5.1 only' — now load-bearing because Fable 5.1 shipped in v2.1.257 as the default Fable model. Also v2.1.259 added the `managedMcpServers` managed setting and changed `allowedMcpServers` to govern only user-added servers. PRIOR PASS: RE-VERIFIED 2026-08-26(b) vs v2.1.246 docs — family-alias substitution, managed paths (all three per-OS paths + the legacy-ProgramData exclusion + managed-settings.d merge), the auto-mode classifier caveat, and the Settings Error/Settings Warning split all re-matched verbatim; settings.md adds a tail: 'so after a -p run that ignores a setting, run claude doctor to see what it dropped.' Adjacent new key in-range: modelPicker (v2.1.243; settings-reference 'Choose which models the /model picker lists', User or managed scope; managed-settings.md: 'the later lineup replaces the earlier one whole') — curates the PICKER, distinct from the availableModels ENFORCEMENT gate; not placed here. PRIOR PASS: RE-VERIFIED 2026-08-26 vs v2.1.233 docs — mechanism unchanged: the v2.1.222 family-alias substitution rule re-matched verbatim (sub-agents.md: 'the newest version of that family the allowlist permits ... Before v2.1.222, Claude Code ran the subagent on the inherited model for a blocked family alias as well'), and the auto-mode classifier caveat re-matched with one added sentence: 'On providers other than the Anthropic API, that Opus fallback runs on the provider's default Opus model without consulting the allowlist' (irrelevant here). DOCS RESTRUCTURE: managed-settings delivery moved to a NEW managed-settings.md page — the three per-OS paths re-matched verbatim there ('/Library/Application Support/ClaudeCode/' macOS, '/etc/claude-code/' Linux/WSL, 'C:\\Program Files\\ClaudeCode\\' Windows; 'Claude Code doesn't read the legacy Windows path C:\\ProgramData\\ClaudeCode\\managed-settings.json'), with managed-settings.d/ drop-ins merged with managed-settings.json. ⚠️ CORRECTION to the baseline's validation model — user/project files are NOT flatly 'rejected as a whole': settings.md now distinguishes, VERBATIM: 'Settings Error: a user, project, or local file has invalid JSON or a value the schema rejects. At the start of an interactive session Claude Code shows a dialog that lets you fix the file with Claude's help, exit, or continue without the broken settings.' vs 'Settings Warning: only individual entries fail, such as a malformed permission rule or an unknown hook event name. Claude Code skips those values and keeps the rest of the file in effect.' And headless, VERBATIM: 'A -p run shows no dialog: Claude Code skips the broken file or values and continues with the rest' — so the silently-dead-hooks failure mode survives ONLY for whole-file JSON/schema errors in -p runs or via continue-without; entry-level mistakes no longer kill the file. PRIOR PASS: RE-VERIFIED 2026-08-09 vs v2.1.226 docs. Scope unchanged: the allowlist applies to the main session model (/model, --model, ANTHROPIC_MODEL, resume), alias resolution, fast mode, skills/commands, the advisor, the background dispatch picker, AND subagent models — 'the model field in subagent frontmatter, the Agent tool's model parameter, ... CLAUDE_CODE_SUBAGENT_MODEL'. Entries match a family alias (sonnet), a version prefix, or a full model ID. CHANGED v2.1.222 — the baseline's flat rule ('an excluded subagent value is SKIPPED and the subagent runs on the inherited model') is now only the fallback branch. sub-agents.md VERBATIM: 'When a blocked value is a family alias such as opus, Claude Code runs the subagent on the newest version of that family the allowlist permits... For any other blocked value, on providers where that substitution doesn't operate, or when the allowlist permits no version of the family, Claude Code runs the subagent on the inherited model instead. Before v2.1.222, Claude Code ran the subagent on the inherited model for a blocked family alias as well.' PRACTICAL EFFECT for a fable-blocking allowlist: blocking fable by omitting it entirely still lands a `model: fable` spawn on the inherited model (no fable version is permitted, so substitution has nothing to land on) — the intended gate still holds. But an allowlist that pins versions (e.g. ['sonnet','claude-opus-4-6']) will now SUBSTITUTE rather than fall back for family aliases, which is a behavior change worth knowing before placing one. v2.1.223 adds a warning when a restricted subagent model causes the parent model to run instead. enforceAvailableModels (v2.1.175+) extends it to the Default picker option. Managed paths (settings.md): macOS /Library/Application Support/ClaudeCode/, Linux/WSL /etc/claude-code/, Windows C:\\Program Files\\ClaudeCode\\; managed-settings.d/*.json drop-ins merge alphabetically. Managed settings parse TOLERANTLY (invalid entries stripped with a warning; /doctor lists them); user/project/local files are STRICT (invalid file rejected as a whole). settings.md lists availableModels as settable in any scope, though model-config frames it as managed/policy — a user-scope allowlist is self-serve but also self-removable.",
      "since_version": "2.1.175 (enforceAvailableModels); 2.1.210 (auto-mode classifier interaction)",
      "doc_urls": [
        "https://code.claude.com/docs/en/model-config.md",
        "https://code.claude.com/docs/en/settings.md",
        "https://code.claude.com/docs/en/managed-settings.md",
        "https://code.claude.com/docs/en/settings-reference.md"
      ],
      "verify": "doc-fetch model-config.md restrict-model-selection + managed-settings.md (paths/drop-ins) + settings.md fix-a-broken-settings-file (RE-VERIFIED 2026-07-17 vs v2.1.212; scope + match rules unchanged). NEW v2.1.210 caveat, RELEVANT TO THIS INSTALL (permissions.defaultMode='auto' + Fable lead) — model-config.md VERBATIM: 'the classifier's Claude Sonnet 5 default applies only when the allowlist permits Sonnet 5. When it's excluded, the classifier runs on the session's model, which the allowlist already governs, or on an Opus model when the session runs on Fable 5.' => if the availableModels gate is ever placed and OMITS sonnet, the auto-mode permission classifier stops running on cheap Sonnet 5 and falls back to the session model (or Opus under a Fable lead) — a silent per-tool-call cost increase on every auto-mode decision. Any allowlist placed here should therefore INCLUDE sonnet. Decide alongside the fable lead-session tradeoff before placing the gate.",
      "affects": [
        {
          "file": "managed-settings.snippet.json",
          "what": "paths, tolerant-parse note, fable tradeoff"
        },
        {
          "file": "settings.snippet.json",
          "what": "strict-validation warning for manual merges"
        }
      ]
    },
    {
      "id": "session-controls",
      "summary": "Background-session / agent-view control surface + claude agents --json schema.",
      "verdict": "CONFIRMED",
      "confidence": "high",
      "current_value": "RE-VERIFIED 2026-09-10 vs v2.1.267 docs — UNCHANGED; the `claude agents --json` table is intact: `cwd`/`kind`/`startedAt` Always, `state` 'One of `working`, `blocked`, `done`, `failed`, or `stopped`' (agent-view.md:707), `pid`/`status` one of busy|waiting|idle (:708), and `waitingFor`'s five values (:709). hooks/watchdog-usage.sh needs no patch. PRIOR PASS: RE-VERIFIED 2026-09-08 vs v2.1.266 docs — the `claude agents --json` field table re-matched verbatim again (cwd/kind/startedAt always; state in working|blocked|done|failed|stopped; waitingFor's five values), so hooks/watchdog-usage.sh needs no patch, and autoContinueAtUsageLimit is still documented with 'Default: true'. NEW CONTROLS IN RANGE, added to session-topology-and-controls.md this pass: `--restricted` / CLAUDE_CODE_RESTRICTED=1 (v2.1.248) removes command/code-execution tools and WebFetch and — note — 'ignores user, project and local settings files', which also disables this bundle's hooks; `--permission-prompts none` (v2.1.259) denies anything that would prompt in unattended headless hosts; `bashOutputMaxChars` / `taskOutputMaxChars` (v2.1.261, up to 128K chars) raise how much tool output lands inline before spilling to a file — raising them costs tokens; `/skill-doctor` (v2.1.261) reports unused loaded skills and their context cost. Also v2.1.260: 'Removed the one-hour time limit on background commands started by subagents; they now run until they exit or are stopped' — an unattended subagent's background command is now unbounded in time. PRIOR PASS: RE-VERIFIED 2026-08-26(b) vs v2.1.246 docs — the `claude agents --json` field table re-matched verbatim AGAIN (cwd/kind/startedAt always; state ∈ working|blocked|done|failed|stopped; waitingFor 5 enumerated values), so hooks/watchdog-usage.sh needs no patch. In-range hygiene fixes: v2.1.246 'Fixed claude agents listing a backgrounded named session twice; backgrounding the same conversation again now numbers the new row' and the just-started-session mid-boot stop fix. ⚠️ NEW COST-RELEVANT SESSION BEHAVIOR IN RANGE: autoContinueAtUsageLimit (v2.1.234+, settings-reference VERBATIM: 'After a claude.ai usage limit stops your session, wait in the open session and continue the task automatically after the reset' — 'Default: true'; scope 'User or managed... a project or local settings file that sets it turns the feature off rather than being ignored') — an over-limit session now resumes burning UNATTENDED at window reset unless opted out; documented in session-topology-and-controls.md + CLAUDE.snippet.md this pass. PRIOR PASS: RE-VERIFIED 2026-08-26 vs v2.1.233 docs — the `claude agents --json` field table re-matched verbatim; waitingFor values enumerated: 'permission prompt', 'input needed', 'sandbox request', 'worker request', 'dialog open'. PRIOR PASS: RE-VERIFIED 2026-08-09 vs v2.1.226 docs — the `claude agents --json` field table is UNCHANGED, so hooks/watchdog-usage.sh (sorts by startedAt, filters state=working, stops via `claude stop`) needs no patch. agent-view.md CLI table: claude agents / --cwd / --json (+--all), claude attach/logs/stop/respawn/rm, claude daemon status/stop. --json entries: cwd, kind, startedAt (camelCase); background entries add id and state ∈ working|blocked|done|failed|stopped; pid/status only while the process is alive (+waitingFor when status=waiting); sessionId/name when set. `claude stop <id>` (alias kill). disableAgentView setting + CLAUDE_CODE_DISABLE_AGENT_VIEW env. STILL UNVERIFIED: CLAUDE_DISABLE_ADOPT, CLAUDE_CODE_DISABLE_BACKGROUND_TASKS (the latter is referenced by sub-agents.md fork-mode notes, so likely real).",
      "since_version": "2.1.139 (agent view preview)",
      "doc_urls": [
        "https://code.claude.com/docs/en/agent-view.md",
        "https://code.claude.com/docs/en/settings.md"
      ],
      "verify": "doc-fetch agent-view.md; run `claude agents --json | jq` and eyeball the fields",
      "affects": [
        {
          "file": "session-topology-and-controls.md",
          "what": "flag list"
        },
        {
          "file": "hooks/watchdog-usage.sh",
          "what": "sorts by startedAt; filters state=working; stops via claude stop"
        }
      ]
    },
    {
      "id": "output-styles",
      "summary": "Output-style file + frontmatter schema.",
      "verdict": "CONFIRMED",
      "confidence": "high",
      "current_value": "RE-VERIFIED 2026-09-10 vs v2.1.267 docs — UNCHANGED; the frontmatter table still lists name / description / `keep-coding-instructions` / `force-for-plugin` (output-styles.md:101-102). No deltas affecting output-styles/terse.md. PRIOR PASS: RE-VERIFIED 2026-09-08 vs v2.1.266 docs — frontmatter table unchanged (name, description, keep-coding-instructions, force-for-plugin). No deltas affecting output-styles/terse.md. PRIOR PASS: RE-VERIFIED 2026-08-26(b) vs v2.1.246 docs — frontmatter table unchanged (keep-coding-instructions and force-for-plugin rows re-matched). In-range wins for terse.md: v2.1.238 'Fixed custom, project, and plugin output styles drifting back to the default voice mid-session' (the style now actually sticks); v2.1.237 added a built-in 'Concise' style (redundant with, not a replacement for, this bundle's terse.md — terse.md keeps the reasoning-preservation carve-outs). PRIOR: RE-VERIFIED 2026-08-09 vs v2.1.226 docs — UNCHANGED. ~/.claude/output-styles/NAME.md (user) or .claude/output-styles/NAME.md (project). Frontmatter: name, description, keep-coding-instructions, force-for-plugin. Activate via /config or settings outputStyle.",
      "since_version": "2.1.73",
      "doc_urls": [
        "https://code.claude.com/docs/en/output-styles.md"
      ],
      "verify": "doc-fetch output-styles.md",
      "affects": [
        {
          "file": "output-styles/terse.md",
          "what": "frontmatter format"
        },
        {
          "file": "settings.snippet.json",
          "what": "outputStyle key"
        }
      ]
    },
    {
      "id": "hooks-context-injection",
      "summary": "UserPromptSubmit and SessionStart can inject additionalContext.",
      "verdict": "CONFIRMED",
      "confidence": "high",
      "current_value": "RE-VERIFIED 2026-09-10 vs v2.1.267 docs — UNCHANGED; the Context-only row still carries `hookSpecificOutput.additionalContext` plus SessionStart's `initialUserMessage`/`watchPaths`/`sessionTitle`/`reloadSkills` (hooks.md:1030), with PostModelSwitch still sharing that row. PRIOR PASS: RE-VERIFIED 2026-09-08 vs v2.1.266 docs — UNCHANGED; the Context-only row still carries hookSpecificOutput.additionalContext plus SessionStart's initialUserMessage/watchPaths/sessionTitle/reloadSkills. PostModelSwitch joins that row (context-only). Newly useful to hooks/version-check.sh: v2.1.251 'SessionStart resume hooks now receive session staleness and the estimated re-cache cost' — a resume-cost signal available for a future nudge; not wired this pass. PRIOR PASS: RE-VERIFIED 2026-08-26(b) vs v2.1.246 docs — UNCHANGED; the Context-only row re-matched verbatim including the SessionStart extras (initialUserMessage, watchPaths, sessionTitle, reloadSkills). In-range: v2.1.239 'Remote sessions keep sending keep-alives while a long SessionStart or Setup hook runs, so the container is not idle-reaped mid-hook'. PRIOR: RE-VERIFIED 2026-08-09 vs v2.1.226 docs — UNCHANGED; the decision-control table still lists SessionStart/Setup/SubagentStart as \"Context only\" with hookSpecificOutput.additionalContext, and SessionStart additionally accepts initialUserMessage, watchPaths, sessionTitle, reloadSkills. Both support hookSpecificOutput.additionalContext (or plain stdout). SessionStart is non-blocking; UserPromptSubmit injects alongside the prompt. SubagentStart also supports additionalContext (injected into the subagent's context) — available for a future per-subagent nudge.",
      "since_version": "unknown",
      "doc_urls": [
        "https://code.claude.com/docs/en/hooks.md"
      ],
      "verify": "doc-fetch hooks.md",
      "affects": [
        {
          "file": "hooks/throttle.sh",
          "what": "UserPromptSubmit additionalContext"
        },
        {
          "file": "hooks/version-check.sh",
          "what": "SessionStart additionalContext"
        }
      ]
    },
    {
      "id": "usage-attribution",
      "summary": "/usage per-feature attribution.",
      "verdict": "CONFIRMED",
      "confidence": "high",
      "current_value": "RE-VERIFIED 2026-09-10 vs v2.1.267 docs — UNCHANGED; the Attribution bullet and the pre-v2.1.222 MCP-share correction re-matched verbatim (costs.md:60), as did the VS Code Account & usage note (:66). PRIOR PASS: RE-VERIFIED 2026-09-08 vs v2.1.266 docs — the Attribution sentence, the MCP-share correction, and the Loops breakdown all re-matched verbatim. NEW in range: costs.md 'Prompt cache (main)' line (v2.1.251) giving request count, share of input tokens from cache, misses and warm state in-terminal — the /cost twin of the statusline prompt_cache object. Reliability: v2.1.261 fixed /usage dropping a model-specific weekly limit row when the usage endpoint is rate limited or right after startup, so a missing row there was a display bug, not zero usage. PRIOR PASS: RE-VERIFIED 2026-08-26(b) vs v2.1.246 docs — attribution sentence re-matched verbatim (now with the v2.1.222 MCP-share correction folded into costs.md itself). NEW SURFACE IN-RANGE — /usage Loops breakdown, costs.md VERBATIM: 'Loops: a row for each of the heaviest /loop or other scheduled tasks that ran recently, ordered by total tokens... Claude Code keys a row by the task's prompt, so a loop you stop and re-create stays one row. Requires Claude Code v2.1.242 or later.' — runaway/chatty /loop tasks are now directly visible in /usage; added to session-topology-and-controls.md. Also v2.1.236: /usage shows the usage-credits spend row for Team/Enterprise (N/A here). PRIOR: RE-VERIFIED 2026-08-09 vs v2.1.226 docs — mechanism unchanged. costs.md VERBATIM: 'Attribution: recent usage attributed to skills, subagents, plugins, and individual MCP servers, each shown as a percentage of the total.' ACCURACY FIX IN RANGE (v2.1.222), VERBATIM: 'An MCP server's share counts only the requests that consumed one of its tool results. Before v2.1.222, after one call to an MCP server, Claude Code attributed every subsequent request to that server, overstating its share.' => any MCP cost share this user read from /usage BEFORE v2.1.222 was inflated and should not be trusted as a baseline; re-measure. VS Code shows the same breakdown with a Day/Week toggle. Requires v2.1.174+.",
      "since_version": "2.1.174; MCP over-attribution fixed 2.1.222",
      "doc_urls": [
        "https://code.claude.com/docs/en/costs.md"
      ],
      "verify": "doc-fetch costs.md",
      "affects": [
        {
          "file": "ADR-claude-code-cost-control.md",
          "what": "findings"
        },
        {
          "file": "README.md",
          "what": "usage habits"
        }
      ]
    },
    {
      "id": "subagent-fanout-caps",
      "summary": "Platform-level ceilings on subagent fan-out: concurrency, nesting depth, lifetime count, and headless spend cap.",
      "verdict": "CONFIRMED",
      "confidence": "high",
      "current_value": "RE-VERIFIED 2026-09-10 vs v2.1.267 docs — UNCHANGED; all four ceilings re-matched verbatim (sub-agents.md:1013 'There's no limit on the total number of subagents Claude can spawn over a session'; :1015 the 20 default plus 'Sessions with ultracode active are exempt: the limit isn't enforced there'; :988 the depth variable; the `--max-budget-usd` row). ➕ Newly relevant: v2.1.267's `maxEffortLevel` is the first documented way to make ultracode UNAVAILABLE, which is the only lever against the one mode that ignores the concurrency cap — see the `max-effort-level` claim. PRIOR PASS: RE-VERIFIED 2026-09-08 vs v2.1.266 docs — all four ceilings re-matched verbatim: concurrency 20 + the ultracode exemption, depth 3, 'There's no limit on the total number of subagents Claude can spawn over a session', and the --max-budget-usd row. Unchanged in range. PRIOR PASS: RE-VERIFIED 2026-08-26(b) vs v2.1.246 docs — all four ceilings re-matched verbatim: concurrency 20 + ultracode exemption ('Sessions with ultracode active are exempt: the limit isn't enforced there'), depth 3, 'There's no limit on the total number of subagents Claude can spawn over a session', and the --max-budget-usd row byte-for-byte. Note in-range: v2.1.239 cost estimates (/cost, status line, --max-budget-usd) now include the 1.1x US-only-inference premium for data-residency workspaces — the dollar cap trips proportionally earlier there (N/A this personal install). ORIGINAL: NEW CLAIM added 2026-08-09 (v2.1.226). These four ceilings did not exist, or had different values, at the v2.1.212 baseline; they are the platform's own fan-out bounds and sit underneath this bundle's guards. (1) CONCURRENCY — sub-agents.md VERBATIM: 'By default, when 20 subagents are running in a session, spawning another with the Agent tool fails with `Concurrent subagent limit reached`, and the error tells Claude not to retry... To change the limit, set CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS to any positive whole number. Sessions with ultracode active are exempt: the limit isn't enforced there. Requires Claude Code v2.1.217 or later.' The ultracode exemption is the cost-relevant trap: ultracode removes BOTH this cap and the Large-workflow warning. (2) DEPTH — default 3 layers, set via CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH (see nested-spawn-hooks). (3) LIFETIME COUNT — REMOVED. sub-agents.md VERBATIM: 'There's no limit on the total number of subagents Claude can spawn over a session', and changelog v2.1.224 VERBATIM: 'Removed the 200-subagent-per-session spawn cap; long-running sessions no longer refuse new agents (concurrency and depth limits still apply).' A long-lived thin-brain session now has NO built-in lifetime ceiling — only burn-rate guards bound it. (4) HEADLESS SPEND CAP — cli-reference.md VERBATIM: '--max-budget-usd | Maximum dollar amount to spend on API calls before stopping (print mode only). Spend from subagents counts toward the cap. Once spend reaches the cap, spawning another subagent fails with `Budget limit reached`, and Claude Code stops background subagents that are still running; the cap-enforcement behaviors require Claude Code v2.1.217 or later.' This is the only hard dollar cap available and it is print-mode only — it does NOT protect interactive sessions, which is precisely the gap guard-usage-budget.sh fills.",
      "since_version": "2.1.217 (concurrency cap + --max-budget-usd enforcement); 2.1.219 (depth default 3); 2.1.224 (lifetime cap removed)",
      "doc_urls": [
        "https://code.claude.com/docs/en/sub-agents.md",
        "https://code.claude.com/docs/en/cli-reference.md",
        "https://code.claude.com/docs/en/changelog.md"
      ],
      "verify": "doc-fetch sub-agents.md#limit-how-many-subagents-run-at-once + cli-reference.md --max-budget-usd row",
      "affects": [
        {
          "file": "CLAUDE.snippet.md",
          "what": "fan-out guidance now names the real platform ceilings + ultracode exemption"
        },
        {
          "file": "session-topology-and-controls.md",
          "what": "control table rows for concurrency/depth/budget"
        },
        {
          "file": "settings.snippet.json",
          "what": "//subagentFanoutEnv note"
        },
        {
          "file": "ADR-claude-code-cost-control.md",
          "what": "evidence row 12"
        }
      ]
    },
    {
      "id": "claude-version-cmd",
      "summary": "How to read the installed Claude Code version.",
      "verdict": "CONFIRMED",
      "confidence": "high",
      "current_value": "RE-VERIFIED 2026-09-10 vs v2.1.267 docs — UNCHANGED ('`--version`, `-v` | Output the version number', cli-reference.md:137); live check: `claude --version` -> '2.1.267 (Claude Code)'. PRIOR PASS: RE-VERIFIED 2026-09-08 vs v2.1.266 docs — UNCHANGED ('--version, -v | Output the version number'); live check: `claude --version` -> '2.1.266 (Claude Code)'. PRIOR PASS: RE-VERIFIED 2026-08-26(b) vs v2.1.246 docs — UNCHANGED (cli-reference.md row: '--version, -v | Output the version number'); live check: `claude --version` → '2.1.246 (Claude Code)'. PRIOR: RE-VERIFIED 2026-08-09 vs v2.1.226 docs — UNCHANGED (cli-reference.md: \"--version, -v | Output the version number\"). `claude --version` (or `-v`) prints a string containing the semver; no public `version` field guaranteed in session/statusline data (app.version available via telemetry OTEL_METRICS_INCLUDE_VERSION).",
      "since_version": "unknown",
      "doc_urls": [
        "https://code.claude.com/docs/en/cli-reference.md"
      ],
      "verify": "run `claude --version`",
      "affects": [
        {
          "file": "hooks/version-check.sh",
          "what": "version parse source"
        }
      ]
    },
    {
      "id": "max-effort-level",
      "summary": "maxEffortLevel caps the session's effort level across every provider, cannot be raised by a lower scope, and below xhigh makes ultracode unavailable.",
      "verdict": "CONFIRMED",
      "confidence": "high",
      "current_value": "NEW CLAIM added 2026-09-10 (v2.1.267). settings-reference.md#maxeffortlevel, VERBATIM: 'Cap the effort level a session can use, leaving lower levels available. Any higher level runs at the cap instead, including one from `/effort`, the `/model` picker, `--effort`, `CLAUDE_CODE_EFFORT_LEVEL`, a skill's or subagent's `effort` frontmatter, or the model's own default.' Changelog v2.1.267, VERBATIM: 'Added `maxEffortLevel` setting (top-level or per model under `modelSettings`): caps the effort level on every provider, including Bedrock, Vertex and Foundry; users can still pick a lower level.' Type: string, one of low|medium|high|xhigh|max ('A `\"max\"` value sets no cap'); Default: 'unset, so no cap applies'; Scope: 'Any file. Deploy it in managed settings to enforce it for an organization. When several scopes set a cap, the lowest applies, so a cap set in one scope can't be raised from another.' Two properties make it a guardrail rather than a preference: (1) the lowest-scope-wins rule means it is enforceable downward and cannot be escaped from a lower scope; (2) VERBATIM 'a cap below `xhigh` makes ultracode unavailable on the models the cap applies to' — the ONLY documented way to disarm ultracode, which is exempt from CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS and from the large-workflow warning. Per-model exemption: 'add `maxEffortLevel` to a model's `modelSettings` entry. That entry replaces this key for the model only within the settings source that sets both.' NOT SET by this bundle — a value is a behavior change to the user's session, so it is documented and PROPOSED, never auto-applied.",
      "since_version": "2.1.267",
      "doc_urls": [
        "https://code.claude.com/docs/en/settings-reference.md",
        "https://code.claude.com/docs/en/model-config.md"
      ],
      "verify": "grep -n 'maxEffortLevel' on settings-reference.md; confirm the scope/lowest-wins bullet and the ultracode sentence",
      "breaks_if_changed": "If the lowest-scope-wins rule or the ultracode sentence changes, the only documented brake on ultracode fan-out disappears and session-topology-and-controls.md overstates the available controls.",
      "affects": [
        {
          "file": "session-topology-and-controls.md",
          "what": "three control rows added 2026-09-10 (cap effort, disarm ultracode, per-model exemption)"
        },
        {
          "file": "CLAUDE.snippet.md",
          "what": "cost-discipline guidance paragraph added 2026-09-10"
        },
        {
          "file": "ADR-claude-code-cost-control.md",
          "what": "verified-findings row 15"
        },
        {
          "file": "settings.snippet.json",
          "what": "NOT wired — proposed HITL only; setting a cap changes session behavior"
        }
      ]
    }
  ]
}
